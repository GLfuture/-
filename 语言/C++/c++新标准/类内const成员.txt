c++类的const成员
const数据成员
	1.具备const特性，常：只读，不可修改
	2.必须初始化
	3.初始化必须采用初始化参数列表的方式初始化	
	构造函数（形参1，形参2....）：数据1（形参1）...{}
const成员函数
	1.注意是写法，const写在被修饰函数的后面
	2.常成员函数不能修改类中成员的数据
	3.常成员函数可以与普通的同名成员函数共存
	普通对象两者均存在时优先调用普通函数
const 对象
	只能调用常成员函数